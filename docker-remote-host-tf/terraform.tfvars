# Specify the Docker host URL to connect to
docker_host = "ssh://myusername@remote-hostname-or-ip"

# Specify the registry address for Docker
registry_address = "myregistry.example.com"

# Specify the path to the Docker registry config file
registry_config_file = "/path/to/my/registry/config.json"
